1. 闭包保存状态: 在使用`useState`Hook时, React会创建一个闭包来保存当前状态值.每次调用useState时, 返回的当前状态值都是通过闭包来访问的

2. 闭包捕获依赖项: 在使用`useEffect`Hook时, React会根据依赖数组中的值来决定何时执行副作用, 依赖数组中的值实际上是在副作用内部通过闭包来访问的

3. 在使用自定义Hooks时, 闭包可以避免重复创建对象和函数, 从而提高性能
